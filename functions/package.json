{
    "name": "receipt-parser-function",
    "version": "1.0.0",
    "description": "Cloud Function to parse receipts using Google Cloud Document AI",
    "main": "parseReceipt.js",
    "scripts": {
        "start": "node parseReceipt.js",
        "deploy": "gcloud functions deploy parseReceipt --runtime nodejs20 --trigger-http --allow-unauthenticated --memory 512MB --timeout 60s",
        "deploy-gen2": "gcloud functions deploy parseReceipt --gen2 --runtime nodejs20 --trigger-http --allow-unauthenticated --memory 512MB --timeout 60s --region us-central1"
    },
    "dependencies": {
        "@google-cloud/documentai": "^8.0.0",
        "express": "^4.18.2"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "devDependencies": {
        "@types/express": "^5.0.6",
        "@types/node": "^25.0.9",
        "ts-node": "^10.9.2",
        "typescript": "^5.9.3"
    }
}
